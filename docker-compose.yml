version: '3.8'

services:
  meilisearch:
    image: getmeili/meilisearch:latest
    container_name: marocloi_search
    ports:
      - "7700:7700"
    environment:
      - MEILI_ENV=development
    volumes:
      - meili_data:/meili_data
    restart: always

volumes:
  meili_data: